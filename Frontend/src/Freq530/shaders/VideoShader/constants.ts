import { VideoShaderSettings } from "./types";

export const VideoShaderDefault: VideoShaderSettings = {
  id: "firstVideoShader",
  shaderId: "firstVideoShader",
  isActive: true,
  name: "First Video Shader Name",
}